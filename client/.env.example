# Client Environment Variables
# Copy this file to .env.local (for local dev) or set in Vercel dashboard (for production)

# URL of your deployed backend (Railway/Render)
# Leave empty to use the Vite dev proxy (http://localhost:5000)
VITE_SERVER_URL=
